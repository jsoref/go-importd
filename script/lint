#!/bin/bash
#
# Sets up all the dependencies, etc. so you can build and test.

set -euo pipefail
# shellcheck source=./utilities.bash
source "$(dirname "$0")/utilities.bash"

header "Lint"
bin/gometalinter --deadline=5m

# vim: set ft=sh :
